import 'package:flutter/material.dart';

class ShowStakeholderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'Detalhes do Stakeholder',
        ),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
